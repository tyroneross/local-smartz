2026-04-14 | `_create_model` in `agent.py` still wrapped ChatOllama in `.with_retry()` even after `pipeline.py::_role_llm` was fixed to drop the wrap. Two independent construction sites, one failure mode. | When a deepagents-related incompatibility is fixed in one factory, grep for every other model-factory in the project and apply the same fix; don't trust a single-site patch. Reference memory: `~/.claude/projects/-Users-tyroneross/memory/reference_deepagents_runnable_retry.md`.
2026-04-14 | Phase 4 overlay fix couldn't be exercised at runtime after Phase 4 co-located the researcher model with the other specialists — the mid-stream swap trigger went away. | When one item in a plan makes another item's test path rare or synthetic, flag it in Phase 5 (Validate) and decide whether to split the commit or accept code-only verification with TAG:UNTESTED.
